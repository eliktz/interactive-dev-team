# Codex CLI configuration for the Paperclip container (per-squad copy).
# Bind-mounted into the paperclip service at /paperclip/.codex/config.toml.
#
# FILL IN base_url with YOUR Azure OpenAI resource before using codex-backed
# Paperclip workers. The placeholder below is deliberately not a live endpoint.
#
# IMPORTANT:
# - api-version is the literal string "preview" (the unified /openai/v1 endpoint
#   does not accept dated versions like "2025-04-01-preview"; that returns HTTP 400).
# - The actual AZURE_OPENAI_API_KEY is NOT in this file. It is passed through the
#   process environment via each agent's adapter_config.env block (or via the
#   paperclip service env in docker-compose.yml / the squad .env for a
#   container-wide default). Never write the key here.

model_provider = "azure"
model = "gpt-5.3-codex"

[model_providers.azure]
name = "Azure OpenAI"
base_url = "https://YOUR-AZURE-RESOURCE.cognitiveservices.azure.com/openai/v1"
env_key = "AZURE_OPENAI_API_KEY"
wire_api = "responses"
query_params = { api-version = "preview" }
